{
  "compilerOptions": {
    "types": ["node", "jest"],
    "target": "ES2023",
    "module": "Node16",
    "moduleResolution": "Node16",
    "strict": true,
    "esModuleInterop": true,
    "rootDir": ".",
    // Type checking only — the shipped bundle is produced by ncc. Without this
    // a bare `tsc -p` scatters compiled .js next to the sources.
    "noEmit": true,
    "plugins": [{ "name": "ts-auto-mock" }]
  },
  "include": ["src/**/*", "bin/**/*"],
  "exclude": ["**/*.test.ts"]
}
